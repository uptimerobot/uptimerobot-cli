version: 2
updates:
  # Bump SHA-pinned GitHub Actions via reviewed PRs so the pins don't go stale.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    groups:
      github-actions:
        patterns:
          - '*'
